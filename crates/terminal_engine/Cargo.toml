# MANAGED BY VAPOR. EDIT Workspace.vapor.toml OR THE ROLE-SPECIFIC CONTENT MANIFEST, THEN RUN `vapor-sdk workspace sync`.
[package]
name = "terminal-engine"
version.workspace = true
edition.workspace = true
# END MANAGED BY VAPOR.

[dependencies]
